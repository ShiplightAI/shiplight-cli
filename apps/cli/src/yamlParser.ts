/**
 * Extended YAML Parser — CLI Wrapper
 *
 * Thin re-export from the shared yaml-transpiler package.
 */

export {
  parseYamlTestFile,
  type ParsedYamlTestFile,
  type ParameterSet,
  type ParsedSuiteTest,
  type ParsedSuite,
} from './yaml-transpiler';
